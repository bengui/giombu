-if (coupon.status === "unredeemed")
	.alert.alert-info(style="text-align:center")
		b Cod. de cupón: #{coupon.code}
		p Estado: No canjeado
		br
		-if(RolesHelper.isPartner(expose.user, deal.store))
			a(href='/deals/redeem_coupon/#{sale._id}/#{coupon.code}', role="button", class="btn space btn-primary") Canjear
-else
	.alert.alert-success
		b #{coupon.code}
		p Estado: Canjeado 	